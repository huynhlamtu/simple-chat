export const searchMessage = (threads, query) => {
  //   const matchMessage = [];
  //   threads.forEach((thread) => {
  //     thread.messages.forEach(function (message) {
  //       if (message.includes(query)) {
  //         matchMessage.push(message);
  //       }
  //     });
  //   });
  //   console.log(matchMessage);
  console.log(query);
};
